The box-sizing property allows us to include the padding and border in an element's total width and height.



alt + SHIFT + down  copy the line to down 

alt + cursor jahan jahan cursor rakhengey multiple functionality dega 

The line string.substring(0, string.length-1); is used to remove the last character from the string string.

